<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloudok.uc.mapper.MessageMapper">
	
	<select id="searchInteractionMessages"
		parameterType="Map" resultType="String">
		select distinct thread_id from uc_message  
		<if test="status == 0">
			where thread_id not in (select distinct t.thread_id from uc_message t where t.status=1 and  thread_id like concat(#{memberId},'-%')  and type in (3,4) and deleted=false )
			and thread_id like concat(#{memberId},'-%')  and type in (3,4) and deleted=false
		</if>
		<if test="status == 1">
			where thread_id  in (select distinct t.thread_id from uc_message t where t.status=1 and thread_id like concat(#{memberId},'-%')  and type in (3,4) and deleted=false )
		</if>
		limit #{start},#{end}
	</select>

	<select id="searchInteractionMessagesCount"
		parameterType="Map" resultType="Long">
		select count( distinct thread_id ) from uc_message  
		<if test="status == 0">
			where thread_id not in (select distinct t.thread_id from uc_message t where t.status=1 and  thread_id like concat(#{memberId},'-%')  and type in (3,4) and deleted=false )
			and thread_id like concat(#{memberId},'-%')  and type in (3,4) and deleted=false
		</if>
		<if test="status == 1">
			where thread_id  in (select distinct t.thread_id from uc_message t where t.status=1 and thread_id like concat(#{memberId},'-%')  and type in (3,4) and deleted=false )
		</if>
	</select>
	
	<select id="searchPrivateMessages"
		parameterType="Map" resultType="String">
		select distinct thread_id from uc_message where ( from_id = #{memberId} or to_id=#{memberId} ) and type in (1,2) and deleted=false limit #{start},#{end}
		 
	</select>

	<select id="searchPrivateMessagesCount"
		parameterType="Map" resultType="Long">
		 select count( distinct thread_id ) from uc_message where ( from_id = #{memberId} or to_id=#{memberId} ) and type in (1,2)  and deleted=false
	</select>
	 
</mapper>