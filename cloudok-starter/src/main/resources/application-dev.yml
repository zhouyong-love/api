debug: true
logging:
  level:
    com.cloudok: debug
spring:
  datasource:
    name: druidDataSource
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/cloudok?autoReconnect=true&characterEncoding=UTF-8&allowMultiQueries=true&useSSL=true&serverTimezone=GMT%2B8
    username: root
    password: 123456
    druid:
      initial-size: 2
      min-idle: 2
      maxActive: 10
      maxWait: 60000
      timeBetweenEvictionRunsMillis: 60000
      minEvictableIdleTimeMillis: 300000
      validationQuery: SELECT 1
      testWhileIdle: true
      testOnBorrow: false
      testOnReturn: false
      poolPreparedStatements: true
      maxPoolPreparedStatementPerConnectionSize: 20
      connectionProperties: druid.stat.mergeSql\=true;druid.stat.slowSqlMillis\=5000
      web-stat-filter:
        enabled: false
        url-pattern: "/*"
        exclusions: "*.js,*.gif,*.jpg,*.bmp,*.png,*.css,*.ico,/druid/*"
      stat-view-servlet: 
        url-pattern: "/druid/*"
        enabled: false
        reset-enable: false
        login-username: root
        login-password: cloudok
        
security: 
  token: 
    secret: 7olodZvQcq8DHiI165oJEPNs53ZkRPhc
    expired: 1800
    signature-secret: lfP2OQ6hlJLhgRPflsLmZv3NZuJJvGLr
    signature-expired: 300

com:
  cloudok: 
    message: 
      register:
        name: "用户注册"
        channel:
          sms:
            template:
              param: classpath*:message/template/user/regsiter/param.flt
              title: classpath*:message/template/user/regsiter/title.flt
              text: classpath*:message/template/user/regsiter/text.flt
            retry:
              enable: false
            parameters:    # 渠道所需参数
              sign-name: 云可    #短信签名
              template-code: SMS_197611035         #短信模板
      login:
        name: "验证码登录"
        channel:
          sms:
            template:
              param: classpath*:message/template/user/login/param.flt
              title: classpath*:message/template/user/login/title.flt
              text: classpath*:message/template/user/login/text.flt
            retry:
              enable: false
            parameters:    # 渠道所需参数
              sign-name: 云可    #短信签名
              template-code: SMS_197611035         #短信模板
    channel: 
      aliyun: 
        sms:
          access-key:
          access-secret:
          region-id: 
        oss:
          endpoint:
          bucket:
          base-dir:
          sign-timeout:
          access-key:
          access-secret:
        voice:
          access-key:
          access-secret:
          region-id: 
          